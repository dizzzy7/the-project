// import createMiddleware from 'next-intl/middleware';

// export default createMiddleware({
// locales: ['en', 'de'], // Supported languages
// defaultLocale: 'en', // Default language
// });

// export const config = {
// matcher: ['/((?!api|_next|.*\\..*).*)'], // Skip static files
// };
