/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // experimental: {
  //   esmExternals: true,
  // },
  // onDemandEntries: {
  //   maxInactiveAge: 5000,
  //   pagesBufferLength: 2,
  // },
  // devIndicators: {
  //   autoPrerender: false,
  // },
  // webpack: (config, { dev, isServer }) => {
  //   if (dev && !isServer) {
  //     config.cache = false;
  //   }
  //   return config;
  // },
};

module.exports = nextConfig;
